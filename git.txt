en git aceptar la invotacion
en la carpeta del tema 
git init
git remote add origin https://github.com/telaio-networks/myrightmood.git
git clone https://github.com/telaio-networks/myrightmood.git para que se descargue

git add index.html 

git commit -m "Mensaje" (el mensaje puede ser Mi primer commit, prueba, etc, suelo poner lo que es que se hizo)


git push origin main


git log --oneline